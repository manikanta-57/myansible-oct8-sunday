---
- hosts: all
  become: yes
  tasks:
  - name: in this tasks , i want to install apache2 webserver
    apt: 
      name: apache2
      state: present
    
  - name: in this tasks, i wnat to start apache2 webserver
    service:
      name: apache2
      state: started